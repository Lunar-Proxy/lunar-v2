---
import ImportLucide from './lucide.astro';
import '@/main.css';

const navButtons = [
  { id: 'back', icon: 'arrow-left' },
  { id: 'forward', icon: 'arrow-right' },
  {
    id: 'refresh',
    icon: 'refresh-cw',
    class: 'group',
    iconClass: 'transition-transform duration-500',
  },
  { id: 'home', icon: 'house' },
];

const menuSections = [
  [
    { icon: 'plus', label: 'New Tab' },
    { icon: 'maximize-2', label: 'Full Screen' },
    { icon: 'refresh-cw', label: 'Reload' },
  ],
  [
    { icon: 'code', label: 'Inspect Element' },
    { icon: 'moon', label: 'Dark Mode' },
    { icon: 'glasses', label: 'AB Cloak' },
  ],
  [
    { icon: 'log-out', label: 'Panic' },
    { icon: 'settings', label: 'Settings' },
  ],
];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="@scripts/tb.ts"></script>
    <script src="@scripts/menu.ts"></script>
    <script src="@scripts/suggestions.ts"></script>
  </head>

  <body class="bg-gray-900 p-3 font-sans text-sm text-white">
    <div class="w-full">
      <div
        id="tab-wrapper"
        class="relative isolate flex h-12 items-end overflow-hidden rounded-t-xl bg-[#1c1a2b] px-1.5"
      >
        <div class="flex w-full items-center gap-1.5">
          <div id="tcontainer" class="flex max-w-full flex-nowrap gap-1.5 overflow-x-auto"></div>
          <button
            id="add"
            class="relative -top-1/2 flex h-7 w-7 items-center justify-center rounded-md text-gray-400 transition hover:bg-gray-700 hover:text-white"
          >
            <i data-lucide="plus" class="h-3.5 w-3.5"></i>
          </button>
        </div>
      </div>

      <div class="flex h-10 items-center gap-3 border-t border-[#3a3758] bg-[#1c1a2b] px-3">
        <div class="flex items-center gap-1.5">
          {
            navButtons.map(({ id, icon, class: btnClass, iconClass }) => (
              <button
                id={id}
                class:list={[
                  'flex h-7 w-7 items-center justify-center rounded-md text-gray-400 transition hover:bg-gray-700 hover:text-white',
                  btnClass,
                ]}
              >
                <i
                  data-lucide={icon}
                  class:list={['h-3.5 w-3.5', iconClass, id === 'refresh' ? 'group-hover:animate-spin' : '']}
                />
              </button>
            ))
          }
        </div>

        <div class="relative flex grow flex-col">
          <div class="relative w-full">
            <div class="absolute top-1/2 left-2.5 z-20 flex -translate-y-1/2 items-center gap-2">
              <i data-lucide="lock" class="h-3.5 w-3.5 text-green-500"></i>
            </div>

            <input
              id="urlbar"
              type="text"
              autocomplete="off"
              class="h-8 w-full rounded-full border border-[#3a3758] bg-[#23213a] pr-10 pl-9 text-[13px] text-gray-200 transition-colors placeholder:text-gray-500 hover:text-white focus:outline-none"
              placeholder="Search or enter address"
            />

            <button
              id="fav"
              class="absolute top-1/2 right-1 flex h-6 w-6 -translate-y-1/2 items-center justify-center rounded-md text-gray-400 transition hover:bg-gray-700 hover:text-white"
            >
              <i data-lucide="bookmark" class="h-4 w-4"></i>
            </button>

            <div
              id="loading-bar"
              class="pointer-events-none fixed inset-x-0 z-50 hidden h-0.5 w-screen rounded-full bg-[#5c59a5] opacity-0 transition-all duration-200"
            >
            </div>
          </div>
        </div>

        <button
          id="menubtn"
          class="flex h-7 w-7 items-center justify-center rounded-md text-gray-400 transition hover:bg-gray-700 hover:text-white"
        >
          <i data-lucide="menu" class="h-4 w-4"></i>
        </button>
      </div>

      <div class="relative">
        <div
          id="menu"
          class="absolute top-full right-0 z-1000 mt-2 hidden w-56 overflow-hidden rounded-xl border border-[#3a3758] bg-[#1f1f30] shadow-2xl backdrop-blur-md"
        >
          {
            menuSections.map((section, i) => (
              <div class:list={['flex flex-col', i !== 0 ? 'border-t border-[#3a3758]' : '']}>
                {section.map(({ icon, label }) => (
                  <button class="menu-item flex w-full items-center gap-3 px-3.5 py-2.25 text-gray-200 transition hover:bg-[#2a293f] hover:text-white">
                    <i data-lucide={icon} class="h-4.5 w-4.5 shrink-0" />
                    <span class="text-[13px] whitespace-nowrap">{label}</span>
                  </button>
                ))}
              </div>
            ))
          }
        </div>
      </div>

      <div id="fcontainer" class="absolute inset-x-0 top-[5.6rem] bottom-0 w-full overflow-hidden rounded-b-lg"></div>

      <ImportLucide />
    </div>
  </body>
</html>
